# .env.local
# Authentication
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key-here  # Generate: openssl rand -base64 32

# GitHub OAuth
GITHUB_ID=your-github-client-id
GITHUB_SECRET=your-github-client-secret
GITHUB_ACCESS_TOKEN=your-github-access-token  # Optional: for API rate limits

# Database
DATABASE_URL=postgresql://username:password@db.example.neon.tech/database

# Note: 
# 1. Create a Neon PostgreSQL database at https://neon.tech
# 2. Get your connection string from Neon dashboard
# 3. For local development, copy this file to .env.local and fill in the values
# 4. After setting up, run:
#    - yarn db:generate (to generate migrations)
#    - yarn db:push (to push schema to database)
#    - yarn db:seed (optional: to seed database with sample data)
